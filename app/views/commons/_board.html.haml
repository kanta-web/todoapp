= link_to board_path(board) do
  .card_container
    .card_title
      = board.title
    .card_content
      = board.content  
    .card_detail 
      -# = image_tag board.user.avatar_image
    

      